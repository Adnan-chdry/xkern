#ifndef GAME_DEMO_H
#define GAME_DEMO_H

/* Runs a playable breakout demo with the game engine.  Returns when the
 * player presses ESC, so the kernel can continue booting. */
void game_demo_run(void);

#endif
